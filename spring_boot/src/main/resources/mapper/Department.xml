<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.szyrm.cache.mapper.DepartmentMapper">
    <insert id="insert" >
        insert into department(departmentName) value(#{departmentName})
    </insert>

</mapper>